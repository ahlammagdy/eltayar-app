import 'package:flutter/material.dart';

class AllTayars extends StatelessWidget {
  const AllTayars({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("All  Tayars"));
  }
}
